/*Mujtaba Waheed
500900260
CPS 393 Assignment 2
November 14th, 2019
I certify that this assignment is entirely my own work
*/
//-------------------

/*
Name: ReadSize
purpose: This function calls the
corresponding function from assign2funcs.c
add and square roots the array elements.
Parameters: @array[] @len
Input: double array[] and int length
Output: returns the square root of the array elements
*/
double readSize(double array[], int len);
//--------------------
/*
Name: normalize
purpose: This function calls the
corresponding function from assign2funcs.c
Parameters: @array[] @len
Input: double array[] and int len
Output: NONE. Just calculates and store value.
*/
void normalize(double array[], int len);
//-------------------
/*
name: printff
purpose: This function calls the
corresponding function from assign2funcs.c
parameters: @vector[] and  @length
Input: double vector[], int length
Output: prints out a well formatted array vector
*/
double printff(double vector[], int length);
//-------------------
/*
name: scanSize
purpose: This function calls the
corresponding function from assign2funcs.c
Parameters: @array and @length
Input: double array[] and int length
Output: NONE. Just scans the array and store the vlaues
*/
double scanSize(double array[], int length);

//-----------------------
/*
name: bufferreader
purpose: This function calls the
corresponding function from assign2funcs.c
Parameters: @void - NONE
Input: @ void - NONE
Output: NONE. Clears the stdin
*/
void bufferreader(void);
canSize(double array[], int length);

